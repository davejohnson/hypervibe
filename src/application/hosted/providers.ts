// The hosted observation surface registers only providers with a bounded,
// credential-injected read capability. It never constructs the CLI registry.
import '../../adapters/providers/railway/railway.adapter.js';
