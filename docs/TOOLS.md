# Tool Catalog

Generated from source at `2026-04-16T22:45:32.440Z`.

Total tools: **144**

_Do not edit this file manually. Re-generate with `npm run docs:tools`._

## appstore.tools.ts

- `appid_capabilities` - List, enable, or disable capabilities on an existing App ID (Bundle ID)
- `appid_register` - Register a new App ID (Bundle ID) on App Store Connect and optionally enable capabilities
- `appstore_prepare_submission_assets` - Create/update App Store localization metadata and upload screenshots for submission readiness.
- `appstore_setup_help` - Get instructions for setting up App Store Connect API
- `appstore_submission_readiness` - Check App Store submission readiness (build, localization metadata, screenshots) for the editable app version.
- `appstore_submit` - Submit an app version for App Store review. The app must have a version in PREPARE_FOR_SUBMISSION state with a valid build attached.
- `testflight_builds` - List recent builds on App Store Connect with their processing and compliance status
- `testflight_compliance` - Wait for a build to finish processing, set export compliance, and optionally distribute to testers. This is required before a build appears in TestFlight.
- `testflight_upload` - Upload an IPA to TestFlight using xcrun altool. After uploading, use testflight_compliance to set export compliance and make the build available to testers.

## approval.tools.ts

- `approval_request_approve` - Approve a pending approval request.
- `approval_request_create` - Create an approval request for a protected operation (deploy, rollback, infra apply).
- `approval_request_get` - Get an approval request by ID.
- `approval_request_list` - List approval requests (optionally filter to pending).
- `approval_request_reject` - Reject a pending approval request.

## run.tools.ts

- `audit_list` - List recent audit events
- `run_get` - Get detailed information about a specific run
- `run_list` - List recent deployment runs

## autofix.tools.ts

- `autofix_error_ignore` - Ignore an error fingerprint (stop auto-fixing it)
- `autofix_errors_list` - List errors detected by the auto-fix agent
- `autofix_status` - Check the status of the auto-fix agent
- `autofix_watch_add` - Enable auto-fix watching for a service. The agent will monitor logs and create PRs for errors.
- `autofix_watch_list` - List all auto-fix watches
- `autofix_watch_remove` - Stop watching a service for auto-fix

## cloudflare.tools.ts

- `cloudflare_dns_create` - Create a DNS record
- `cloudflare_dns_delete` - Delete a DNS record
- `cloudflare_dns_list` - List DNS records for a domain. Provide either zoneId or domain name.
- `cloudflare_dns_update` - Update an existing DNS record
- `cloudflare_dns_upsert` - Create or update a DNS record by name and type (idempotent operation)
- `cloudflare_setup_help` - Get instructions for creating a Cloudflare API token with the correct permissions
- `cloudflare_zones_list` - List all domains (zones) in the Cloudflare account

## local.tools.ts

- `component_create` - Add a component (database, cache) to an environment
- `component_list` - List components in an environment
- `local_bootstrap` - Generate compose.yaml and .env.local files for local development

## connection.tools.ts

- `connection_create` - Create or update a provider connection (e.g., Railway API token, Stripe keys, Cloudflare, SendGrid)
- `connection_delete` - Delete a provider connection
- `connection_list` - List all provider connections and available providers
- `connection_verify` - Verify that a provider connection works

## db.tools.ts

- `db_migrate` - Run database migrations on a deployed environment
- `db_migrate_presets` - List available migration command presets
- `db_migrate_provider` - Staged Postgres provider migration with plan/copy/cutover phases, verification, and rollback guardrails.
- `db_migrate_provider_rollback` - Rollback database cutover by restoring DATABASE_URL and DIRECT_URL from DATABASE_URL_PREV or stored component history.
- `db_move` - Move/switch your app database to another provider by provisioning target DB and switching env vars with preview+confirm.
- `db_provision` - Provision a managed database (default: Supabase Postgres; also supports Railway), save it as a component, and optionally sync env vars to hosting.
- `db_query` - Run a SQL query against a database. By default, only SELECT queries are allowed. Use allowMutations=true to enable INSERT/UPDATE/DELETE (with confirmation).
- `db_reset` - Reset a database by dropping all tables. Preview mode shows tables and row counts; confirm=true executes the reset. Terminates active connections first.
- `db_url` - Get the database connection URL for an environment (for local migrations or debugging)

## deploy.tools.ts

- `deploy` - Deploy services to an environment (staging, production, etc.)
- `deploy_rollback` - Rollback by redeploying services from the most recent successful deploy run.
- `deploy_status` - Check the status of a deployment run
- `service_create` - Create a new service in a project
- `service_list` - List services in a project

## github.tools.ts

- `deploy_branch_setup` - Set up branch-based deploy workflows for a provider using the project\
- `github_ai_review_setup` - Create a GitHub Actions workflow for AI-powered PR code review using Claude. Two-step: preview shows planned changes, confirm creates workflow + sets secret.
- `github_branch_protection_get` - Get branch protection rules for a branch
- `github_branch_protection_set` - Create or update branch protection rules (two-step: preview then confirm)
- `github_pages_setup` - Set up GitHub Pages with a custom domain (two-step: preview then confirm)
- `github_pages_status` - Get the GitHub Pages status for a repository
- `github_secret_delete` - Delete a repository secret
- `github_secret_set` - Set or update a repository secret
- `github_secrets_list` - List repository secret names (values are never exposed)
- `github_setup_help` - Get instructions for creating a GitHub personal access token with the correct permissions
- `github_workflow_create` - Create a workflow from common templates (two-step: preview then confirm)
- `github_workflow_runs` - Get recent runs for a workflow
- `github_workflow_trigger` - Manually trigger a workflow (requires workflow_dispatch trigger)
- `github_workflows_list` - List workflows in a repository

## environment.tools.ts

- `env_create` - Create a new environment for a project (e.g., local, staging, production)
- `env_delete` - Delete an environment
- `env_get` - Get details of a specific environment
- `env_list` - List environments for a project

## logs.tools.ts

- `errors_recent` - Get recent errors from production. Auto-detects project and environment.
- `logs_build` - Get build logs for a deployment
- `logs_deployments` - List recent deployments for a service with their status
- `logs_errors_summary` - Get a summary of recent errors across services in an environment
- `logs_service` - Get runtime logs from a deployed service
- `logs_stripe_webhooks` - Check recent Stripe webhook delivery attempts and failures

## infra.tools.ts

- `infra_apply` - Apply persisted desired state (or explicit state) to create/update provider infrastructure. Use this for new setup and retries; use project_import only to adopt already-existing live projects.
- `infra_desired_get` - Read persisted desired stack state for a project.
- `infra_desired_set` - Persist desired stack state on project policies for team/repeatable apply flows.
- `infra_plan` - Generate a desired-state plan (Terraform-style) for Railway + DB + DNS + SendGrid.
- `stack_bootstrap` - Bootstrap full web stack quickly: project/env/service, DB provisioning, deploy, optional DNS and SendGrid.

## env.tools.ts

- `integration_add` - Add an integration to your project. Provide the required credentials and they will be set across all environments.
- `integration_info` - Get detailed information about an integration, including required variables and setup instructions
- `integration_list_available` - List available integration plugins that can be added to your project
- `integration_scopes` - Get recommended API scopes for an integration based on desired features
- `vars_get` - Get environment variables from a specific environment
- `vars_set` - Set environment variable(s) across environments. Use scope to target specific environments.

## integration.tools.ts

- `integration_keys_delete` - Delete stored integration keys.
- `integration_keys_get` - Get stored integration keys. Returns key names and masked values.
- `integration_sync` - Sync integration keys to environment(s). Supports Stripe keys synced to Railway environments.
- `integration_verify` - Verify integration keys are correctly set in an environment. Fetches actual env vars from Railway and checks expected keys exist with correct prefixes.

## marketing.tools.ts

- `marketing_search_console_verify_file` - Create/update Google Search Console HTML verification file in public/.
- `marketing_seo_scan` - Scan project SEO/search-indexing readiness (routes, sitemap, robots, framework hints).
- `marketing_seo_setup` - Create/update robots.txt and sitemap.xml for SEO indexing readiness.

## project.tools.ts

- `project_create` - Create a new Hypervibe project record for new infrastructure provisioning (use infra_apply to create resources on providers).
- `project_delete` - Delete an infrastructure project
- `project_get` - Get details of a specific project
- `project_import` - Adopt an already-deployed Railway project into Hypervibe state (discovery/import). Not for creating new infrastructure or retrying failed applies.
- `project_intent_get` - Get intent overview (hosting + integrations) derived from current project data.
- `project_list` - List all infrastructure projects
- `project_policy_get` - Get project policy settings
- `project_policy_set` - Set project policy controls (protected environments, approval requirements, desired state).

## recaptcha.tools.ts

- `recaptcha_status` - Check reCAPTCHA connection status and configuration
- `recaptcha_sync` - Sync reCAPTCHA keys to a Railway environment
- `recaptcha_test` - Test a reCAPTCHA token (for debugging)

## secrets.tools.ts

- `secrets_audit` - View secret access audit log
- `secrets_get` - Get a secret value (returns masked). Use the returned secret reference for mapping to env vars.
- `secrets_list` - List secrets from a connected secret manager
- `secrets_map` - Map a secret reference to an environment variable for a project. The secret will be resolved and injected during deployment.
- `secrets_mapping_delete` - Delete a secret-to-env-var mapping
- `secrets_mappings_list` - List all secret-to-env-var mappings for a project
- `secrets_rotate` - Rotate a secret and propagate the new value to all mapped environments. Only supported by some providers (e.g., AWS Secrets Manager).
- `secrets_set` - Create or update a secret in a secret manager
- `secrets_sync` - Resolve secret references and sync them to environment(s) on the hosting platform

## sendgrid.tools.ts

- `sendgrid_domain_dns_records` - Get the required DNS records for a SendGrid domain authentication
- `sendgrid_domain_setup_dns` - Auto-create Cloudflare DNS records for SendGrid domain authentication (two-step: preview then confirm)
- `sendgrid_domain_validate` - Re-validate DNS records for a SendGrid domain authentication
- `sendgrid_domains_list` - List domain authentications with their validation status
- `sendgrid_email_send` - Send a test email via SendGrid
- `sendgrid_setup` - Set up SendGrid for an environment: sync API key to Railway and optionally configure webhook
- `sendgrid_webhook_disable` - Disable SendGrid event webhook
- `sendgrid_webhook_events` - List available SendGrid webhook event types
- `sendgrid_webhook_get` - Get current SendGrid event webhook settings
- `sendgrid_webhook_set` - Configure SendGrid event webhook URL and events
- `sendgrid_webhook_test` - Send a test event to the configured webhook URL

## setup.tools.ts

- `setup_configure` - Configure a Railway service with proper settings (start command, migrations, health checks, repo/branch deploy source)
- `setup_scan` - Scan an existing Railway project and identify configuration issues

## stripe.tools.ts

- `stripe_data_diff` - Compare products/prices between two Stripe environments to see what would be synced
- `stripe_data_sync` - Sync products and prices from one Stripe environment to another
- `stripe_products_list` - List products in a Stripe environment (sandbox or live)
- `stripe_sandbox_clear` - Clear all customer records from Stripe sandbox. Two-step: preview first, then confirm to delete.
- `stripe_setup_help` - Get instructions for setting up Stripe API keys and local webhook testing (no Stripe CLI needed)
- `stripe_webhook_create` - Create or update a webhook endpoint in Stripe. Returns the webhook signing secret on creation.
- `stripe_webhook_delete` - Delete a webhook endpoint from Stripe
- `stripe_webhook_events` - List common webhook events that can be subscribed to
- `stripe_webhook_setup` - Create a Stripe webhook and sync the signing secret to a Railway environment in one step
- `stripe_webhooks_list` - List all webhook endpoints configured in Stripe

## tunnel.tools.ts

- `tunnel_list` - List all active webhook tunnels
- `tunnel_start` - Start a webhook tunnel to expose a local port to the internet (for testing webhooks from Stripe, SendGrid, etc.)
- `tunnel_status` - Get the status of a specific tunnel
- `tunnel_stop` - Stop a running webhook tunnel

## visualize.tools.ts

- `visualize` - Generate a 3D infrastructure visualization as a self-contained HTML file

## xcode.tools.ts

- `xcode_deploy` - Build an Xcode project and install to a connected device. If no deviceId is provided, returns the device list. If no scheme is provided, discovers the project first.
- `xcode_devices` - List devices available on network/USB for Xcode deployment
- `xcode_discover` - Find workspace, project, and schemes in a directory

